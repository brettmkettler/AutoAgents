import shutil

def move_file(src, dst):
    shutil.move(src, dst)

# NOTE: 'source_file_path' and 'destination_path' are placeholders.
# Replace 'source_file_path' with the path of the file you want to move.
# Replace 'destination_path' with the path of the directory you want to move the file to.
move_file('source_file_path', 'destination_path')